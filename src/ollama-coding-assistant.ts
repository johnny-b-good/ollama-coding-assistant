import { buildDatabase, queryDatabase } from "./actions";

async function main() {
  // TODO: command line arguments
  // await buildDatabase();
  // await queryDatabase();
}

main().catch((err) => {
  console.error(err);
});
